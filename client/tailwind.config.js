/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  darkMode: 'class', // ← 다크모드: .dark 클래스로 제어
  theme: { extend: {} },
  plugins: [],
}
